let num1 = Number(prompt('Digite um numero'));
let num2 = Number(prompt('Digite novamente outro valor'));

const resultado = num1 + num2;

alert(`O resultádo de ambos os valores introduzidos é: ${resultado}.`);
