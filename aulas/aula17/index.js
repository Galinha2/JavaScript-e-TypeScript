function nomePessoa(nome) {
    return(`Olá ${nome}`);
}

const nomeNovo = nomePessoa('Henrique');

nomePessoa('joão');

function soma(x, y) {
    resultado = x + y;
    return resultado;
}

console.log(soma(2, 9));
